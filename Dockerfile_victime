FROM ubuntu:20.04

COPY init-victime-script.sh /path/inside/container/init-victime-script.sh
COPY script/main_victime.py /path/inside/container/main_victime.py 
COPY script/data.txt /path/inside/container/data.txt
COPY script/data1.txt /path/inside/container/data1.txt
RUN apt-get update \
    && apt-get install -y iputils-ping \
    && apt-get install -y iproute2 \
    && apt install net-tools \
    && apt-get install nano -y \
    && apt-get install -y python3 python3-pip libpcap-dev \
    && pip3 install scapy \
    && chmod +x /path/inside/container/init-victime-script.sh \
    && chmod +x /path/inside/container/main_victime.py \
    && chmod +x /path/inside/container/data.txt \
    && chmod +x /path/inside/container/data1.txt \




